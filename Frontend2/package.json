{
  "name": "contactmate",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i ./index.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./index.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}